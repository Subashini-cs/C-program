#include<stdio.h>
#include<conio.h>
int main()
{
    char a[10],b[10],c[10],d[10];
    scanf("%s",&a);
    scanf("%s",&b);
    scanf("%s",&c);
    scanf("%s",&d);
    scanf("%s %s %s %s",puts(a),puts(b),puts(c),puts(d));
    return 0;
    }
